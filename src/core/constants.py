# Telegram API limits
TG_MESSAGE_LIMIT = 4096       # Hard limit for message text
TG_SAFE_MESSAGE_LIMIT = 4000  # Safe limit leaving room for service text (headers, etc.)
TG_CAPTION_LIMIT = 1024       # Hard limit for media caption

# Application version — single source of truth.
# Bump minor (+0.1.0) for features/fixes, major (+1.0.0) for breaking changes.
APP_VERSION = "1.1.0"
